#!/bin/bash

PROG="@top_builddir@/test/test_axi_ns_con_cyl_flow"

INPUT="@top_srcdir@/test/input_files/axi_con_cyl_flow.in"

PETSC_OPTIONS="-pc_factor_levels 4 -sub_pc_factor_levels 4"

# FIXME: Even with a ton of ILU levels, the linear solver doesn't
# converge quickly when splitting these systems onto 12 processors
#${LIBMESH_RUN:-} $PROG $INPUT $PETSC_OPTIONS
$PROG $INPUT $PETSC_OPTIONS
